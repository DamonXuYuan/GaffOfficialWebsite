import React from "react";
import styles from "./style.module.scss";

const ShopProfile: React.FC = () => {
  return (
    <section className={styles.shopProfile}>
      <div className={styles.banner}>店铺介绍</div>
    </section>
  );
};

export default ShopProfile;
